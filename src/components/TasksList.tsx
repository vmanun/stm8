import { Box, Heading } from "@chakra-ui/react";
import React from "react";

export default function TasksList() {
  // TODO: Create single task model, determine how single view should work, design task card
  return (
    <Box>
      <Heading>Current task estimation batch</Heading>
      <Box></Box>
      <Heading>Older batches</Heading>
      <Box></Box>
    </Box>
  );
}
